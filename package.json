{
  "name": "bailo",
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "dev": "turbo run dev",
    "style": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "workspaces": [
    "shared",
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "prettier": "^2.8.4",
    "turbo": "^1.8.2"
  }
}
